/**
 * @note هذا الملف لا يُنفَّذ في static export mode
 *
 * الـ Frontend يتصل مباشرة بـ Express:
 *   POST http://localhost:3000/api/hotels/search
 *
 * هذا الملف محفوظ للمرجعية وللاستخدام عند التحول
 * إلى standalone server مستقبلاً.
 *
 * انظر: server.js → app.post('/api/hotels/search', ...)
 */

export {};
